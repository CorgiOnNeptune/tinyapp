<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link align-self-center" href="/urls">My URLs</a>
        <a class="nav-item nav-link align-self-center" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <div class="nav navbar-nav navbar-right">
      <% if (!currentUser || currentUser === undefined) { %>
        <form class="form-inline" action="/login" style="padding:10px;">
          <button type="submit" class="btn btn-dark">Login</button>
        </form>
        <form class="form-inline" action="/register">
          <button type="submit" class="btn btn-secondary">Register</button>
        </form>
      <% } else { %>
        <form class ="form-inline" action="/logout" method="POST">
        <p style="margin:auto; padding: 10px;">Hello, <%= currentUser.email %>!</p>
          <button type="submit" class="btn btn-outline-dark">Logout</button>
      </form>
      <% } %>
    </div>
  </nav>
</header>